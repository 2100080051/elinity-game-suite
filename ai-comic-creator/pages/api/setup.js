import { setupComic } from '../../lib/store';
export default async function handler(req,res){ if(req.method!=='POST') return res.status(405).json({error:'Method not allowed'}); const { sessionId, theme, style, panels, tone } = req.body||{}; try{ const st = await setupComic(sessionId, { theme, style, panels:Number(panels||6), tone }); res.status(200).json(st); }catch(e){ res.status(400).json({error:e.message}); } }
